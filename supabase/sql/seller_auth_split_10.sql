-- ══════════════════════════════════════════════════════════
--  9단계 — 셀러 비밀번호 분리 (2/2: 원본 컬럼 제거)
--  적용일: 2026-07-23
--
--  ⚠ 선행조건 — seller_auth_split_09.sql 적용 후 아래를 모두 통과해야 실행할 것
--     · seller_auth 건수 = sellers.login_pw 건수, 이관 누락 0
--     · 가입 → 로그인 → 틀린비번 거부 → 본인 비번변경 → 옛비번 거부 →
--       새비번 로그인 → 관리자 재설정 → 재설정 비번 로그인 (전부 통과 확인함)
--     · 공개 키로 seller_auth 조회 시 401 (확인함)
--     · login_pw 에 의존하는 뷰·인덱스·제약 없음 (확인함)
--
--  이 시점부터 sellers 를 통째로 조회해도 비밀번호는 나올 수 없다.
--  2026-07-23 오전 사고(select=* 로 해시 18개 유출)의 구조적 원인이 제거된다.
-- ══════════════════════════════════════════════════════════

alter table public.sellers drop column login_pw;
