# ── 비밀키가 들어있는 파일 (절대 GitHub에 올리지 말 것) ──
CLAUDE.md
HANDOFF.md
*.env
.env*

# ── 작업 도구가 만드는 파일 ──
.claude/
supabase/.temp/
supabase/.branches/

# ── 윈도우/맥이 멋대로 만드는 파일 ──
Thumbs.db
desktop.ini
.DS_Store
